# คู่มือการติดตั้งระบบจัดซื้อวัสดุพัสดุ (Production Installation & Deployment Guide)

คู่มือนี้ระบุขั้นตอนแบบละเอียดตั้งแต่ศูนย์ (Step-by-Step) สำหรับการติดตั้งระบบบนเซิร์ฟเวอร์จริง (Production Server) โดยอ้างอิงตามสถาปัตยกรรมและรายละเอียดโครงสร้างพื้นฐานของคุณดังนี้:
*   **Frontend & API Server (Node.js Express App):** รันบน IP **`10.1.0.15:3000`** (หรือตามไอพีเซิร์ฟเวอร์หลักของคุณ) ให้บริการผ่าน subpath **`/MatPlan`**
*   **Database Server (MySQL):** ติดตั้งบน IP **`10.1.0.201`** พอร์ต `3306` (ชื่อฐานข้อมูล: `MatPlan`)

> 💡 **หมายเหตุสำหรับการอัปเดตระบบที่มีอยู่แล้ว:**
> หากท่านมีระบบเดิมติดตั้งอยู่แล้วบนเซิร์ฟเวอร์ และต้องการอัปเดตระบบเดิมขึ้นเป็นเวอร์ชันปัจจุบัน สามารถอ่านขั้นตอนการอัปเดตแบบละเอียดได้ในไฟล์ [UPDATE.md](./UPDATE.md)

---

## สารบัญ (Table of Contents)
1.  [การเตรียมเครื่องและระบบฐานข้อมูล MySQL (`10.1.0.201`)](#1-การเตรียมเครื่องและระบบฐานข้อมูล-mysql-1010201)
2.  [การเตรียมเครื่องและติดตั้งระบบฝั่ง Application (`10.1.0.15`)](#2-การเตรียมเครื่องและติดตั้งระบบฝั่ง-application-101015)
3.  [การตั้งค่า Environment Variables (`.env`)](#3-การตั้งค่า-environment-variables-env)
4.  [การ Build และการรันเซิร์ฟเวอร์ในโหมด Production](#4-การ-build-และการรันเซิร์ฟเวอร์ในโหมด-production)
5.  [การตั้งค่า Nginx Reverse Proxy (สำหรับ Subpath `/MatPlan`)](#5-การตั้งค่า-nginx-reverse-proxy-สำหรับ-subpath-matplan)
6.  [วิธีการทดสอบระบบและ API](#6-วิธีการทดสอบระบบและ-api)
7.  [สถาปัตยกรรมและขีดความสามารถในการรองรับผู้ใช้งาน (10-20 คนพร้อมกัน)](#7-สถาปัตยกรรมและขีดความสามารถในการรองรับผู้ใช้งาน-10-20-คนพร้อมกัน)
8.  [ระบบการแจ้งเตือนสดและการยืนยันตัวตน (SSE Real-time & JWT Authentication)](#8-ระบบการแจ้งเตือนสดและการยืนยันตัวตน-sse-real-time--jwt-authentication)

---

## 1. การเตรียมเครื่องและระบบฐานข้อมูล MySQL (`10.1.0.201`)

ฝั่งเซิร์ฟเวอร์ฐานข้อมูลให้เข้าใช้ฐานข้อมูล MySQL และสั่งการคำสั่ง SQL ดังนี้:

### 1.1 สร้าง Database และสิทธิ์ผู้ใช้งาน
ล็อกอินเข้าใช้งาน MySQL ในฐานะ root แล้วรันคำสั่ง SQL ต่อไปนี้:

```sql
-- 1. สร้างฐานข้อมูลสำหรับการสำรวจและงบประมาณวัสดุ
CREATE DATABASE IF NOT EXISTS MatPlan CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 2. สร้างตารางสำหรับเก็บข้อมูลสถานะของระบบ (ระบบใหม่จะทำการขยายตารางอื่นเพิ่มให้อัตโนมัติเมื่อเริ่มแอปครั้งแรก)
USE MatPlan;

CREATE TABLE IF NOT EXISTS system_state (
  id INT PRIMARY KEY DEFAULT 1,
  state_data LONGTEXT NOT NULL,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 3. กำหนดสิทธิ์ผู้ใช้เพื่อให้เครื่อง App Server (10.1.0.15) เชื่อมต่อเข้ามาได้
-- (แนะนำให้ตั้งรหัสผ่านที่มีความปลอดภัยสูงแทน 'your_secure_password')
CREATE USER 'root'@'10.1.0.15' IDENTIFIED BY 'your_secure_password';
GRANT ALL PRIVILEGES ON MatPlan.* TO 'root'@'10.1.0.15';
FLUSH PRIVILEGES;
```

> **ข้อแนะนำเพิ่มเติม:** ในไฟล์ตั้งค่า MySQL ของเครื่อง `10.1.0.201` (ไฟล์ `my.cnf` หรือ `/etc/mysql/mysql.conf.d/mysqld.cnf`) ตรวจสอบให้แน่ใจว่าได้เปิดให้รับการเชื่อมต่อภายนอก โดยบรรทัด `bind-address` จะต้องไม่ถูกผูกเฉพาะ `127.0.0.1` ให้ปรับเป็น:
> ```ini
> bind-address = 0.0.0.0
> ```
> และเปิดพอร์ต `3306` ใน Firewall ของเครื่องฐานข้อมูลให้ IP `10.1.0.15` สามารถเชื่อมต่อได้

---

## 2. การเตรียมเครื่องและติดตั้งระบบฝั่ง Application (`10.1.0.15`)

เตรียมเครื่อง Application Server ให้พร้อมใช้งานด้วย Node.js:

### 2.1 ติดตั้ง Node.js (เวอร์ชัน 18 ขึ้นไปแนะนำ)
รันคำสั่งบน Ubuntu/Debian:
```bash
# อัปเดตแพ็คเกจ
sudo apt update && sudo apt upgrade -y

# ดาวน์โหลดและติดตั้ง Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# ตรวจสอบเวอร์ชันหลังติดตั้ง
node -v
npm -v
```

### 2.2 โคลนโค้ดและเตรียมโครงสร้างโฟลเดอร์
```bash
# นำโค้ดโปรเจกต์มาวางไว้ที่ไดเรกทอรี /var/www/MatPlan
sudo mkdir -p /var/www/MatPlan
sudo chown -R $USER:$USER /var/www/MatPlan
cd /var/www/MatPlan

# นำไฟล์โค้ดของท่านลงในโฟลเดอร์นี้
```

### 2.3 ติดตั้ง Dependencies
```bash
npm install
```

---

## 3. การตั้งค่า Environment Variables (`.env`)

สร้างไฟล์ชื่อว่า `.env` ไว้ที่โฟลเดอร์หลักของโปรเจกต์ (`/var/www/MatPlan/.env`) เพื่อระบุรายละเอียดการเชื่อมต่อที่ถูกต้อง:

```bash
nano .env
```

**ระบุข้อมูลลงในไฟล์ `.env` ดังนี้:**
```env
# ตั้งค่าสถานะแวดล้อมเป็น production
NODE_ENV=production

# พอร์ตสำหรับรัน Backend Server
PORT=3000

# URL สำหรับอ้างอิงตัวระบบ
APP_URL="http://10.1.0.15:3000/MatPlan"

# การเชื่อมต่อฐานข้อมูล MySQL (เครื่องฐานข้อมูลปลายทาง)
DB_HOST=10.1.0.201
DB_USER=root
DB_PASSWORD=your_secure_password
DB_NAME=MatPlan

# คีย์สำหรับการเข้ารหัสความปลอดภัย JWT Token สำหรับระบบล็อกอิน
JWT_SECRET=MatPlan_SecretKey_2026_SecureKey_ChangeThis
```

### 3.2 การสร้างฐานข้อมูลและผู้ใช้งานความปลอดภัยสูง (Best Practices)
เพื่อความปลอดภัยสูงและสอดคล้องกับมาตรฐานความมั่นคงปลอดภัยสารสนเทศ **ไม่ควรใช้สิทธิ์ Root** ในการเชื่อมต่อระบบจริง ให้เข้าใช้งานเซิร์ฟเวอร์ MySQL/MariaDB และรันคำสั่ง SQL ต่อไปนี้เพื่อสร้างฐานข้อมูลและกำหนดสิทธิ์ให้เฉพาะเจาะจงแก่ผู้ใช้งานแอปพลิเคชัน:

1. **สร้างฐานข้อมูลสำหรับโครงการ:**
   ```sql
   CREATE DATABASE IF NOT EXISTS MatPlan CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```

2. **สร้างผู้ใช้งานและจำกัดการเข้าถึงอย่างปลอดภัย:**
   - หากเซิร์ฟเวอร์เว็บแอปพลิเคชันอยู่ที่ IP `10.1.0.15` (แนะแนวทางจำกัด Host เพื่อความปลอดภัยสูงสุด):
     ```sql
     CREATE USER 'MatPlan'@'10.1.0.15' IDENTIFIED BY 'รหัสผ่านที่ปลอดภัยอย่างน้อย16ตัวขึ้นไป';
     ```
   - หรือหากต้องการให้ผู้ใช้นี้เข้าถึงจากวงเครือข่ายภายในอื่นๆ ได้ทั่วไป:
     ```sql
     CREATE USER 'MatPlan'@'%' IDENTIFIED BY 'รหัสผ่านที่ปลอดภัยอย่างน้อย16ตัวขึ้นไป';
     ```

3. **กำหนดสิทธิ์เฉพาะเจาะจง (Principle of Least Privilege):**
   กำหนดสิทธิ์ให้ผู้ใช้ `MatPlan` สามารถจัดการได้เฉพาะฐานข้อมูล `MatPlan` เท่านั้น โดยมีสิทธิ์ในการดึงข้อมูล บันทึกข้อมูล ปรับปรุงข้อมูล และสิทธิ์ระดับโครงสร้างที่จำเป็นต่อการเตรียมตารางเบื้องต้น:
   ```sql
   GRANT SELECT, INSERT, UPDATE, DELETE, CREATE, DROP, INDEX, ALTER ON MatPlan.* TO 'MatPlan'@'10.1.0.15'; -- หรือ 'MatPlan'@'%' ตามโฮสต์ที่ตั้งไว้
   ```

4. **ปรับใช้สิทธิ์ทันที:**
   ```sql
   FLUSH PRIVILEGES;
   ```

---

## 4. การ Build และการรันเซิร์ฟเวอร์ในโหมด Production

ระบบนี้ใช้ **Vite (Frontend)** และ **Express (Backend)** ร่วมกันอย่างสมบูรณ์แบบ ดังนั้นต้องทำการแปลงโค้ดให้พร้อมใช้งานจริง (Build) ก่อนรันเสมอ

### 4.1 สั่ง Build โปรเจกต์
```bash
npm run build
```
การรันสคริปต์นี้จะทำหน้าที่:
1.  เรียกใช้งาน `vite build` เพื่อแปลงโค้ด React (Frontend) ให้ออกมาเป็นไฟล์ Static (HTML/JS/CSS) ที่ผ่านการย่อขนาด (Minified) อยู่ในโฟลเดอร์ `dist/` โดยกำหนดให้ Base URL อยู่ที่ `/MatPlan/` โดยสมบูรณ์
2.  เรียกใช้งาน `esbuild` เพื่อรวบรวมโค้ดและไลบรารีของ `server.ts` ออกมาเป็นไฟล์ `dist/server.cjs` เพื่อความรวดเร็วในการรันและรองรับการทำงานในฝั่งโปรดักชันอย่างเสถียร

### 4.2 ตรวจสอบการทำงานของ Backend & Frontend (รันชั่วคราว)
```bash
# รันเพื่อทดสอบการทำงานเบื้องต้น
npm start
```
เมื่อแสดงข้อความ `Server running on port 3000` และข้อความการเชื่อมต่อไปยัง MySQL สำเร็จ ระบบจะสามารถเข้าถึงได้ทันที

### 4.3 ติดตั้ง PM2 สำหรับควบคุมกระบวนการ (Process Manager) ให้รันแบบพื้นหลัง 24/7
เพื่อช่วยให้โปรแกรมทำงานอย่างต่อเนื่องแม้ปิดหน้าจอ Terminal หรือเซิร์ฟเวอร์รีสตาร์ทตัวเอง:
```bash
# ติดตั้ง PM2 ทั่วทั้งระบบ
sudo npm install -g pm2

# สั่งให้แอปพลิเคชันทำงานด้วย PM2
pm2 start dist/server.cjs --name "MatPlan"

# ตั้งค่าให้ PM2 สตาร์ทอัตโนมัติเมื่อบูตเครื่อง Linux
pm2 startup
# (ระบบจะขึ้นคำสั่งโค้ดสีเหลืองให้คัดลอกมาวาง แล้วกดรันเพื่อเปิดใช้งาน)

# บันทึกสถานะการรันปัจจุบันไว้
pm2 save
```

**คำสั่งจัดการสถานะเพิ่มเติม:**
*   `pm2 status` : ดูสถานะการรันปัจจุบัน
*   `pm2 logs MatPlan` : ดู Log การทำงานและการเชื่อมต่อฐานข้อมูลของระบบแบบสดๆ
*   `pm2 restart MatPlan` : รีสตาร์ทระบบเมื่อมีการเปลี่ยนแปลงโค้ด
*   `pm2 delete MatPlan` : สั่งลบโปรเซสออกจาก PM2 (ต้องรันคำสั่งนี้ก่อนรัน `pm2 start` ใหม่ในการอัปเดตระบบ)

---

## 5. การตั้งค่าเว็บเซิร์ฟเวอร์ (การทำงานโดยไม่ต้องติดตั้ง Nginx)

สำหรับระบบของคุณที่มีแอปพลิเคชันอื่นรันอยู่แล้วบน PM2 (เช่น `ERPHOSOFFICE`, `hospital-dashboard-api`, `med-error-api` และพอร์ต `8080`) **การติดตั้งและใช้งาน Git ร่วมกับ PM2 จะไม่มีผลกระทบใดๆ ต่อระบบเดิมที่รันอยู่ 100%** 

และคุณสามารถรันระบบ **MatPlan** โดย **ไม่ต้องใช้ Nginx** ได้อย่างปลอดภัยสูงสุด โดยการรันแยกพอร์ตผ่าน PM2 ตามสเต็ปด้านล่างนี้:

### 💡 วิธีรัน MatPlan ผ่าน PM2 โดยไม่พึ่ง Nginx (แนะนำและปลอดภัยที่สุดต่อระบบเดิม)
วิธีนี้ตัว Node.js (Express) จะทำหน้าที่ประมวลผลทั้ง API และส่งไฟล์หน้าบ้าน React ให้ผู้ใช้อัตโนมัติในตัว โดยรันผ่านพอร์ตอิสระที่ว่างอยู่ (เช่น พอร์ต `3000` หรืออื่นๆ ที่ไม่ชนกับระบบเดิม):

1. **ตั้งค่าไฟล์ `.env` สำหรับพอร์ตเฉพาะเจาะจง (เช่น 3000):**
   เปิดและแก้ไขไฟล์ `.env` ที่โฟลเดอร์ของ MatPlan:
   ```env
   PORT=3000
   APP_URL="http://10.1.0.15:3000/MatPlan"
   ```
   *(หมายเหตุ: หากพอร์ต 3000 ถูกโปรแกรมอื่นใช้งานอยู่แล้ว คุณสามารถเปลี่ยนเป็นพอร์ตใดก็ได้ตามสะดวก เช่น `3001`, `3005`, `5000` เป็นต้น โดยจะไม่มีผลกระทบกับแอปพลิเคชันตัวอื่น)*

2. **สั่งรันระบบด้วย PM2:**
   รันคำสั่งสั่งงานโปรเจกต์ MatPlan:
   ```bash
   pm2 start dist/server.cjs --name "MatPlan"
   pm2 save
   ```

3. **ผู้ใช้งานสามารถเข้าถึงระบบได้ทันทีผ่านพอร์ตนั้นๆ:**
   * ตัวอย่างเช่น: **`http://10.1.0.15:3000/MatPlan`**

---

### ⚠️ ข้อชี้แจงด้านผลกระทบ (Impact Analysis)

* **การติดตั้ง Git:**
  - **ผลกระทบ:** **ไม่มีผลกระทบใดๆ ทั้งสิ้น (0%)** ต่อระบบ `ERPHOSOFFICE` หรือระบบอื่นๆ เนื่องจาก Git เป็นเพียงเครื่องมือจัดการเวอร์ชันของไฟล์โค้ด (Version Control) ไม่ใช่โปรเซสที่ทำงานค้างบนเบรกราวด์หรือแย่งพอร์ตเครือข่าย

* **การไม่ติดตั้ง Nginx:**
  - **ผลกระทบ:** **ไม่มีผลกระทบใดๆ (0%)** ระบบ MatPlan ถูกออกแบบโครงสร้างมาให้สามารถรันแบบ Single Standalone Node.js Web Server ได้ทันทีผ่าน PM2
  - **ข้อดี:** ไม่ต้องเสี่ยงไปวุ่นวายกับการคอนฟิก Nginx หรือกลัวว่าจะไปปิดทับพอร์ต `80/8080` ที่ระบบเดิมใช้อยู่ ช่วยลดความเสี่ยงจากการเซ็ตระบบขัดข้องได้เป็นอย่างดี

---

### 5.2.1 ติดตั้ง Nginx (ทางเลือกเสริมกรณีต้องการซ่อนเลขพอร์ต :3000)
```bash
sudo apt install nginx -y
```

### 5.2.2 ตั้งค่าการส่งต่อ Path (Proxy Pass)
ทำการแก้ไขไฟล์ตั้งค่าเริ่มต้นของ Nginx:
```bash
sudo nano /etc/nginx/sites-available/default
```

วางบล็อกคำสั่ง `location` ต่อไปนี้ไว้ภายในบล็อก `server { listen 80; ... }` ของไฟล์คอนฟิก:

```nginx
server {
    listen 80;
    server_name 10.1.0.15;

    # สำหรับให้บริการระบบตัวนี้ภายใต้ Subpath /MatPlan
    location /MatPlan/ {
        proxy_pass http://127.0.0.1:3000/MatPlan/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # กรณีพยายามเรียก /MatPlan (แบบไม่มีสแลชต่อท้าย) ให้ย้ายเส้นทางให้เหมาะสม
    location = /MatPlan {
        return 301 $scheme://$http_host/MatPlan/;
    }

    # เส้นทางอื่นๆ นอกเหนือจากนี้ (กำหนดได้ตามสะดวก)
    location / {
        try_files $uri $uri/ =404;
    }
}
```

### 5.2.3 ทดสอบและเปิดใช้งาน Nginx
```bash
# ตรวจสอบความถูกต้องของไวยากรณ์ไฟล์ตั้งค่า
sudo nginx -t

# รีสตาร์ทการบริการของ Nginx เพื่อเริ่มทำงานตามการตั้งค่าใหม่
sudo systemctl restart nginx
sudo systemctl enable nginx
```
ตอนนี้คุณจะสามารถเรียกดูระบบได้โดยตรงจาก: **`http://10.1.0.15/MatPlan`**

---

## 6. วิธีการทดสอบระบบและ API

หลังจากเชื่อมต่อและเปิดบริการเรียบร้อย คุณสามารถทดสอบการเชื่อมต่อ API ว่าตอบกลับและประมวลผลได้ถูกต้องผ่านเครื่องมือเช่น **Postman**, **Insomnia** หรือใช้คำสั่ง **`curl`** บน Terminal:

### 6.1 ทดสอบสถานะภาพรวมของ API (Health Check)
```bash
curl -i http://10.1.0.15:3000/MatPlan/api/health
```

### 6.2 ทดสอบเรียกดู State และข้อมูลทั้งหมดของระบบ (GET `/state`)
ใช้เพื่อดึงข้อมูลผู้ใช้งาน, ข้อมูลคำขอพัสดุ และสถานะแผนภาพรวมทั้งหมดจาก MySQL ออกมาตรวจสอบ:
```bash
curl -i http://10.1.0.15:3000/MatPlan/api/state
```
หรือดึงจาก URL หลัก (ผ่าน Nginx):
```bash
curl -i http://10.1.0.15/MatPlan/api/state
```
**ผลลัพธ์ที่ควรได้รับ:** ข้อมูลแบบ JSON ขนาดใหญ่ที่แสดงรายละเอียดครบถ้วนเกี่ยวกับ `users`, `requests`, `departments` เป็นต้น แสดงว่าการเชื่อมต่อฐานข้อมูล MySQL ฝั่งเครื่อง `10.1.0.201` ทำงานสำเร็จสมบูรณ์

---

## 7. สถาปัตยกรรมและขีดความสามารถในการรองรับผู้ใช้งาน (10-20 คนพร้อมกัน)

ระบบนี้รองรับการทำงานของผู้ใช้พร้อมกันระดับ 10-20 คน (หรือสูงสุดได้ถึงหลายร้อยคนพร้อมกัน) ได้อย่างสบายและมีประสิทธิภาพสูงสุด ด้วยเหตุผลด้านการออกแบบสถาปัตยกรรมดังนี้:

1.  **การเชื่อมต่อแบบ Pool Connection:**
    ระบบใช้การเชื่อมต่อ MySQL ด้วยเทคโนโลยี `mysql.createPool` ซึ่งมีการจัดการปริมาณการเชื่อมต่อให้อัตโนมัติ (Connection Pool) จำกัด Connection Limit เริ่มต้นไว้ที่ `10` ช่องทาง เพื่อลดภาระ (Overhead) ในการเชื่อมต่อฐานข้อมูลใหม่ทุกครั้ง และนำช่องทางที่ใช้งานเสร็จแล้วมาวนใช้ซ้ำทันที
2.  **ประสิทธิภาพระดับสูงของ Node.js:**
    ตัวรันระบบฝั่ง Backend สร้างบนสถาปัตยกรรม Non-blocking I/O ของ Node.js (สถาปัตยกรรมแบบ Event-driven) ทำให้สามารถประมวลผล Request หลายสิบรายการเข้ามาในวินาทีเดียวกันได้โดยไม่สะดุดหรือทำให้ระบบช้าลงเลย
3.  **กลไกหน่วยความจำแคชภายใน (In-Memory State Caching):**
    *   เพื่อป้องกันไม่ให้ฐานข้อมูล MySQL ปลายทางรับภาระหนักในการคิวรีข้อมูลซ้ำๆ ฝั่ง Node.js Server จะเก็บสำเนาสถานะชุดล่าสุดไว้ใน RAM (เรียกว่า `dbCache`)
    *   เมื่อผู้ใช้เปิดหน้าจอหรือมีการ Polling ดึงสถานะ ระบบจะส่งข้อมูลจาก RAM ในเซิร์ฟเวอร์ตอบกลับไปทันที (ใช้เวลาตอบสนองเร็วในระดับมิลลิวินาที < `5ms`)
    *   และเมื่อมีการเขียนหรือเปลี่ยนแปลงข้อมูลเข้ามา ระบบจะอัปเดตข้อมูลในหน่วยความจำแคชทันที และสั่งอัปเดตบันทึกลง MySQL ด้านหลังแบบอะซิงโครนัส (Asynchronous background write)

---

## 8. ระบบการแจ้งเตือนสดและการยืนยันตัวตน (SSE Real-time & JWT Authentication)

ระบบนี้พัฒนาเทคโนโลยี **การแจ้งเตือนสดด้วย Server-Sent Events (SSE)** และ **ระบบความปลอดภัย JWT** เพื่อเพิ่มประสิทธิภาพสูงสุด:

1.  **ระบบการยืนยันตัวตนความปลอดภัยสูง (JWT Authentication):**
    *   รหัสผ่านได้รับการเข้ารหัสความปลอดภัยด้วยเกณฑ์มาตรฐาน
    *   ระบบออกสิทธิ์ในรูปแบบ **JWT (JSON Web Token)** และเก็บบันทึกบน Browser Client เพื่อใช้ยืนยันตนในการเรียก API ทุกครั้งโดยอัตโนมัติ
2.  **การแจ้งเตือนสดแบบ Server-Sent Events (SSE Stream):**
    *   เซิร์ฟเวอร์สร้างการเชื่อมต่อช่องทางพิเศษ `GET /MatPlan/api/notifications/stream` ค้างไว้กับผู้ใช้งานทุกคน
    *   เมื่อมีการเพิ่มคำขอ อนุมัติ หรือตีกลับแก้ไขรายการ ระบบจะยิงข้อความ Push Notification ส่งตรงไปแสดงบนหน้าจอของผู้ใช้งานกลุ่มเป้าหมาย (เช่น หัวหน้ากลุ่มงาน, เจ้าหน้าที่พัสดุ) ในทันทีแบบ Real-time โดยไม่ต้องพึ่งพาการส่ง Polling Request ทุกกี่วินาทีอีกต่อไป
3.  **การสืบค้นข้อมูลประสิทธิภาพสูงด้วย Server-side Pagination (Live MySQL Explorer):**
    *   มีหน้าจอ **⚡ Live MySQL Explorer** ในเมนูฝ่ายพัสดุสำหรับดึงข้อมูลคำขอพัสดุตรงจาก MySQL ผ่านคำสั่ง `LIMIT` และ `OFFSET` 
    *   รองรับการค้นหา (Search), การกรองตามฝ่าย/แผนก (Department Filter), กรองตามหมวดหมู่ (Category Filter) และกรองตามสถานะ (Status Filter) ช่วยให้เปิดดูและจัดการข้อมูลคำขอนับหมื่นรายการได้อย่างรวดเร็ว ไม่กินแรมฝั่งเบราว์เซอร์
